# Workato Platform CLI
.workatoenv

# Workato recipe projects (user-specific, not shared)
projects/

# Custom connectors (organization-specific, not shared)
connectors/

# Connector SDK secrets
master.key
settings.yaml
settings.yaml.enc

# Resource provider settings (environment-specific)
.resource-providers.yml

# General secrets (prevent accidental commits)
.env
.env.*
*.key
*.pem
*.secret
*.credential*

# Local working artifacts
.claude/worktrees/
__pycache__/
